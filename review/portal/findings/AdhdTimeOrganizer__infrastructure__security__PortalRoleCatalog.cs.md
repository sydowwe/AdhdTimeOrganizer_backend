# Review: AdhdTimeOrganizer/infrastructure/security/PortalRoleCatalog.cs
Role: config
Summary: Small, well-documented role catalog that correctly derives role names from the framework's `UserRoleEnum` via `nameof(...)` rather than duplicating string literals — single source of truth is respected.
Coverage: n/a

## Issues
No issues found.
